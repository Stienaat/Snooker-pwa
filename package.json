{
  "name": "fs-snooker-online",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/main.js && node --check src/online.js && node --check src/table.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "ws": "^8.18.3" }
}
